<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>My Profile</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
	</head>

	<body class="bg-light">
		<%- include('../partials/teacherNavbar', { user }) %>

		<div class="container mt-4">
			<div class="card shadow-sm">
				<div class="card-body">
					<div class="d-flex align-items-center gap-4">
						<img
							src="<%= teacher.profilePic?.url || '/images/default-avatar.png' %>"
							class="rounded-circle"
							width="90"
							height="90"
						/>

						<div>
							<h4 class="mb-1"><%= teacher.fullName %></h4>
							<p class="text-muted mb-0"><%= teacher.email %></p>
							<p class="text-muted mb-0"><%= teacher.phone || '—' %></p>
						</div>
					</div>

					<hr />

					<div class="row g-3">
						<div class="col-md-6">
							<label class="text-muted">Qualification</label>
							<p class="fw-semibold">
								<%= teacher.qualification.degree %> – <%= teacher.qualification.field %>
							</p>
						</div>

						<div class="col-md-6">
							<label class="text-muted">Institution</label>
							<p class="fw-semibold"><%= teacher.qualification.institution || '—' %></p>
						</div>

						<div class="col-md-6">
							<label class="text-muted">Subjects</label>
							<p class="fw-semibold"><%= teacher.subjects.join(', ') %></p>
						</div>

						<div class="col-md-6">
							<label class="text-muted">Experience</label>
							<p class="fw-semibold"><%= teacher.experienceYears %> years</p>
						</div>

						<div class="col-md-6">
							<label class="text-muted">Hourly Rate</label>
							<p class="fw-semibold">₹ <%= teacher.hourlyRate %></p>
						</div>

						<div class="col-md-6">
							<label class="text-muted">Status</label>
							<span class="badge bg-success text-uppercase"><%= teacher.status %></span>
						</div>
					</div>
				</div>
			</div>
		</div>

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
