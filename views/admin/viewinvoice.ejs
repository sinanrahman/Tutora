<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Invoice - <%= newInvoice.id %></title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  
  <style>
    /* Add your custom sidebar css link here if needed */
    
    .invoice-card {
      border: none;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
    }
    
    .invoice-header {
      background-color: #870216; /* Your Theme Color */
      color: white;
      border-radius: 0.375rem 0.375rem 0 0;
    }

    .status-badge {
      font-size: 0.9rem;
      padding: 0.5em 1em;
      border-radius: 50rem;
    }

    /* PRINT STYLES: Hides Sidebar/Navbar when user clicks Print */
    @media print {
      body * {
        visibility: hidden;
      }
      .mobile-topbar, .sidebar, .navbar {
        display: none !important;
      }
      #invoice-container, #invoice-container * {
        visibility: visible;
      }
      #invoice-container {
        position: absolute;
        left: 0;
        top: 0;
        width: 100%;
        margin: 0;
        padding: 0;
        box-shadow: none;
      }
      .no-print {
        display: none !important;
      }
    }
  </style>
</head>

<body class="bg-light">

  <div class="d-md-none mobile-topbar fixed-top bg-light shadow-sm no-print">
    <div class="d-flex align-items-center justify-content-between px-3" style="height: 60px;">
      <img src="/image/logo.png" alt="Logo" class="mobile-logo-img" style="height: 40px;">
      <button class="btn btn-light" onclick="toggleSidebar()">
        <i class="bi bi-list fs-4"></i>
      </button>
    </div>
  </div>

 

  <div class="main-content p-4 p-md-5">
    
    <nav aria-label="breadcrumb" class="no-print mb-4">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/admin/Dashboard">Home</a></li>
        <li class="breadcrumb-item"><a href="/admin/viewinvoice">Invoices</a></li>
        <li class="breadcrumb-item active" aria-current="page">Invoice #<%= newInvoice.id %></li>
      </ol>
    </nav>

    <div class="card invoice-card" id="invoice-container">
      
      <div class="invoice-header p-4 d-flex justify-content-between align-items-center">
        <div>
          <h2 class="mb-0 fw-bold">INVOICE</h2>
          <small class="opacity-75">Transaction Receipt</small>
        </div>
        <div class="text-end">
          <h4 class="mb-0">#<%= newInvoice.id %></h4>
        </div>
      </div>

      <div class="card-body p-4 p-md-5">
        
        <div class="row mb-5">
          <div class="col-sm-6 mb-3 mb-sm-0">
            <h6 class="text-muted text-uppercase fw-bold mb-3">Bill To:</h6>
            <h5 class="fw-bold text-dark">Student ID: <%= newInvoice.studentId || 'N/A' %></h5>
            
            <% if(newInvoice.paid) { %>
              <span class="badge bg-success status-badge mt-2">
                <i class="bi bi-check-circle-fill me-1"></i> PAID
              </span>
            <% } else { %>
              <span class="badge bg-warning text-dark status-badge mt-2">
                <i class="bi bi-exclamation-circle-fill me-1"></i> UNPAID
              </span>
            <% } %>
          </div>

          <div class="col-sm-6 text-sm-end">
            <h6 class="text-muted text-uppercase fw-bold mb-3">Invoice Details:</h6>
            <p class="mb-1">
              <strong>Date:</strong> 
              <%= new Date(newInvoice.date).toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' }) %>
            </p>
            <% if(newInvoice.description) { %>
              <p class="mb-1 text-muted small fst-italic">
                "<%= newInvoice.description %>"
              </p>
            <% } %>
          </div>
        </div>

        <div class="table-responsive mb-4">
          <table class="table table-striped border-top">
            <thead class="table-light">
              <tr>
                <th scope="col" style="width: 10%">#</th>
                <th scope="col" style="width: 60%">Item Description</th>
                <th scope="col" class="text-end" style="width: 30%">Amount</th>
              </tr>
            </thead>
            <tbody>
              <% if (newInvoice.item && newInvoice.item.length > 0) { %>
                <% newInvoice.item.forEach((item, index) => { %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= item.type || 'Miscellaneous' %></td>
                    <td class="text-end fw-semibold">₹<%= Number(item.amount).toFixed(2) %></td>
                  </tr>
                <% }) %>
              <% } else { %>
                <tr>
                  <td colspan="3" class="text-center text-muted py-3">No items listed</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>

        <div class="row">
          <div class="col-md-6 offset-md-6">
            <div class="bg-light p-3 rounded">
              <div class="d-flex justify-content-between align-items-center">
                <span class="text-muted fw-bold text-uppercase">Total Amount</span>
                <span class="fs-3 fw-bold" style="color: #870216;">
                  ₹<%= Number(newInvoice.amount).toFixed(2) %>
                </span>
              </div>
            </div>
          </div>
        </div>

        <div class="mt-5 pt-4 border-top text-center text-muted small">
          <p class="mb-0">Thank you for your business.</p>
          <p>If you have any questions about this invoice, please contact the administration.</p>
        </div>

      </div>
    </div>

    <div class="d-flex justify-content-end gap-2 mt-4 no-print">
      <a href="/admin/viewinvoice" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to List
      </a>
      
      <a href="/admin/invoice/download/<%= newInvoice.id %>" class="btn btn-primary" style="background-color: #870216; border-color: #870216;">
        <i class="bi bi-file-earmark-pdf-fill me-1"></i> Download PDF
      </a>
    </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
  
  <script>
    function toggleSidebar() {
      // Assuming you have an element with id="sidebar" in your partial
      const sidebar = document.getElementById('sidebar');
      if(sidebar) sidebar.classList.toggle('active');
    }
  </script>
</body>
</html>