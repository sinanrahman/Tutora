<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Parent Navbar</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    :root {
      --bloodstone-primary: #5d0d18;
      --bloodstone-hover: #8b1a22;
      --custard-bg: #fff9eb;
      --shadow-soft: 0 10px 30px rgba(93, 13, 24, 0.12);
      --radius: 16px;
    }

    body {
      padding-top: 70px;
      font-family: 'Poppins', sans-serif;
    }

    /* NAVBAR */
    .parent-navbar {
      background: rgba(253, 253, 252, 0.9);
      backdrop-filter: blur(12px);
      box-shadow: var(--shadow-soft);
      height: 65px;
    }

    .navbar-logo {
      width: 75px;
    }

    .panel-badge {
      text-transform: capitalize;
      color: black !important;
      font-size: 1.300rem;
      font-weight: 600;
    }

    .nav-link {
      font-weight: 500;
      padding: 0.5rem 1rem;
      border-radius: 12px;
      transition: 0.3s;
    }

    .nav-link:hover {
      background: rgba(93, 13, 24, 0.08);
      color: var(--bloodstone-primary);
    }

    /* PROFILE */
    .profile-img {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid white;
      box-shadow: var(--shadow-soft);
    }

    /* OFFCANVAS */
    .offcanvas {
      background: rgba(253, 253, 252, 0.9);
    }

    .offcanvas .nav-link {
      font-size: 1rem;
      margin-bottom: 0.5rem;
    }

    .logout-btn {
      /* background-color: #c90000; */
      /* background: linear-gradient(135deg, var(--bloodstone-primary), var(--bloodstone-hover)); */
      background: linear-gradient(135deg, black, rgb(49, 47, 47));

      border: none;
      color: white;
      border-radius: var(--radius);
      padding: 0.75rem;
      font-weight: 600;
      width: 100%;
    }

    .nav-item.active .nav-link {
      background-color: #c90000;
      color: #fff !important;
      font-weight: 600;
    }

    .profile-pill {
      display: flex;
      align-items: center;
      gap: 10px;
      background: linear-gradient(135deg, #0b0f1a, #111827);
      color: #fff;
      border: 1px solid rgba(255, 255, 255, 0.15);
      padding: 8px 16px;
      border-radius: 999px;
      font-weight: 500;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.35);
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .profile-pill:hover {
      transform: translateY(-1px);
      box-shadow: 0 10px 28px rgba(0, 0, 0, 0.45);
    }

    /* red circular icon */
    .profileee {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, #c90000, #ff3b3b);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .profileee i {
      font-size: 1.1rem;
      color: #fff;
    }
  </style>
</head>

<body>
  <nav class="navbar fixed-top navbar-expand-lg parent-navbar">
    <div class="container-fluid">

      <!-- Logo -->
      <div class="d-flex align-items-center">
        <a class="navbar-brand" href="/parent/dashboard">
          <img src="/image/logo.png" class="navbar-logo">
        </a>
        <span class="panel-badge d-none d-lg-inline">Parent Panel</span>
      </div>

      <!-- Mobile toggle -->
      <button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#parentMenu">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- DESKTOP MENU -->
      <div class="collapse navbar-collapse d-none d-lg-flex">
        <ul class="navbar-nav ms-auto align-items-center gap-2">

          <li class="nav-item <%= activePage === 'dashboard' ? 'active' : '' %>">
            <a class="nav-link" href="/parent/dashboard">Dashboard</a>
          </li>

          <li class="nav-item <%= activePage === 'payments' ? 'active' : '' %>">
            <a class="nav-link" href="/parent/payment">Payments</a>
          </li>

          <li class="nav-item <%= activePage === 'reports' ? 'active' : '' %>">
            <a class="nav-link" href="/parent/viewreport">Reports</a>
          </li>

          <li class="nav-item <%= activePage === 'sessions' ? 'active' : '' %>">
            <a class="nav-link" href="/parent/sessions">Class History</a>
          </li>
        </ul>
        <button class="btn btn-dark shadow-sm border-light ms-3 profile-pill" data-bs-toggle="modal"
          data-bs-target="#logoutModal">
          <span class="profileee">
            <i class="bi bi-person-fill"></i>
          </span>
          Logout

        </button>
      </div>
    </div>

    </div>
  </nav>
  <div class="modal fade" id="logoutModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Confirm Logout</h5>
          <button class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body text-center py-4">
          <i class=" fs-1 text-danger mb-3"></i>
          <p class="mb-0 fs-5">Are you sure you want to logout?</p>
        </div>

        <div class="modal-footer justify-content-center">
          <button class="btn btn-outline-secondary" data-bs-dismiss="modal">
            Cancel
          </button>
          <a href="/logout" class="btn btn-danger">Logout</a>
        </div>
      </div>
    </div>
  </div>

  <!-- MOBILE OFFCANVAS -->
  <div class="offcanvas offcanvas-end" id="parentMenu">
    <div class="offcanvas-header">
      <h5 class="panel-badge">Parent Panel</h5>
      <button class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>

    <div class="offcanvas-body d-flex flex-column">
      <ul class="navbar-nav mb-4">
        <li class="nav-item <%= activePage === 'dashboard' ? 'active' : '' %>"><a class="nav-link"
            href="/parent/dashboard">Dashboard</a></li>
        <li class="nav-item <%= activePage === 'payments' ? 'active' : '' %>"><a class="nav-link"
            href="/parent/payment">Payments</a></li>
        <li class="nav-item <%= activePage === 'reports' ? 'active' : '' %>"><a class="nav-link"
            href="/parent/viewreport">Reports</a></li>
        <li class="nav-item <%= activePage === 'sessions' ? 'active' : '' %>"><a class="nav-link"
            href="/parent/sessions">Class History</a></li>
      </ul>

      <div class="mt-auto text-center">
        <i class="bi bi-person-circle fs-1"></i>
        <h6 class="fw-semibold">
          <%= user?.fullName %>
        </h6>
        <a href="/logout" class="logout-btn mt-3 d-block">
          <i class="bi bi-box-arrow-right me-2"></i>Logout
        </a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>