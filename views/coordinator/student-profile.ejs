<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>Student Profile</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

	</head>
	<body>
		<%-include('../admin/partials/navbar') %>  <%- include('../partials/sidebar') %>

		<div class="container mt-5">
			<div class="row d-flex justify-content-center mt-5">
				
				<div class="col-4 mt-5">
					<nav style="--bs-breadcrumb-divider:'>';" aria-label="breadcrumb">
        <ol class="breadcrumb p-3">
          <li class="breadcrumb-item "><a href="/coordinator/Dashboard">Home</a></li>
          <li class="breadcrumb-item active" aria-current="page">studentProfile</li>
        </ol>
      </nav>
					<h3 class="fw-bold text-center text-secondary">Student Profile</h3>

					<div class="card shadow-sm mt-3">
						<div class="card-body">
							<p>
								<strong>Name:</strong>
								<%= student.fullName %>
							</p>
							<p>
								<strong>ID:</strong>
								<%= student._id %>
							</p>
							<p>
								<strong>Class:</strong>
								<%= student.standard %>
							</p>
							<p>
								<strong>Phone:</strong>
								<%= student.phone %>
							</p>
							<p>
								<strong>Country:</strong>
								<%= student.country %>
							</p>

							<hr />

							<h5>Assigned Teachers</h5>
							<% if (student.assignedTeachers.length === 0) { %>
							<p class="text-muted">No teachers assigned</p>
							<% } else { %>
							<ul>
								<% student.assignedTeachers.forEach(t => { %>
								<li>
									<strong><%= t.fullName %></strong>
									<% if (Array.isArray(t.subjects)) { %> - <%= t.subjects.join(', ') %> <% } %>
								</li>
								<% }) %>
							</ul>
							<% } %>

							<hr />

							<h5>Total Attended Hours</h5>
							<p class="fs-5 fw-bold text-primary"><%= totalHours %> hours</p>
							<div class="button__container d-flex justify-content-between">
								<a href="/coordinator/dashboard" class="btn btn-secondary">
									Back
								</a>
								<a href="/coordinator/update-teacher/<%=student._id%>" class="btn btn-primary">
									Update Teacher
								</a>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>
