<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Parent Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />

</head>

<style>
  .profile-icon {
    color: #444;
  }

  .performancebadge {
    color: #e9d203;
    font-size: large;
  }

  .main-content {
    margin-top: 0px;
    padding: 30px;
  }

  @media (max-width: 768px) {

    .main-content {
      margin-left: 0;
      padding: 12px;
      margin-top: 6px;
    }
    .main-content h4 {
      font-size: 17px;
    }
    .performance-card {
      padding: 14px;
      min-height: unset;
    }

    .performance-card h2 {
      font-size: 22px;
    }

    .performance-card .rounded-circle {
      width: 52px;
      height: 52px;
      font-size: 16px;
      border-width: 4px;
    }
    .card.shadow-lg {
      box-shadow: 0 6px 18px rgba(0, 0, 0, .08);
    }

    .card.shadow-lg .px-4 {
      padding-left: 14px !important;
      padding-right: 14px !important;
    }

    .card.shadow-lg .py-3 {
      padding-top: 12px !important;
      padding-bottom: 12px !important;
    }

    .card.shadow-lg .row.g-3 {
      gap: 10px;
      padding: 14px;
    }

    .card.shadow-lg .col-md-4>div {
      padding: 12px;
    }

    .card.shadow-lg .fs-2 {
      font-size: 22px !important;
    }

    .card.shadow-lg p {
      font-size: 13px;
      line-height: 1.4;
    }

    .package-card {
      padding: 16px;
    }

    .package-card h6 {
      font-size: 14px;
    }

    .plan-box {
      padding: 12px;
    }

    .days-left {
      padding: 12px;
    }

    .days-left span {
      font-size: 26px;
    }

    .h-100,
    .h-60 {
      height: auto !important;
    }

    .card.shadow-lg .row.g-3 {
      flex-wrap: nowrap;
    }

    .card.shadow-lg .row.g-3>div {
      flex: 0 0 auto;
    }

    .report-summary {
      display: none;
    }

    .report-date {
      display: block;
      font-size: 12px;
      color: #6c757d;
      margin-top: 4px;
    }
  }


  .performance-card {
    background: #940707;
    color: #fff;
    border-radius: 20px;
    padding: 20px;
    min-height: 120px;
  }


  .package-card {
    background: #fff;
    border-radius: 20px;
    padding: 24px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
  }

  .package-pill {
    background: #fff3cd;
    color: #b26a00;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 600;
  }

  .plan-box {
    background: #000;
    color: #fff;
    border-radius: 14px;
    padding: 18px;
  }

  .days-left {
    background: #fffbe6;
    border-radius: 14px;
    padding: 18px;
    border: 2px solid #ffe58f;
    text-align: center;
  }

  .days-left span {
    font-size: 42px;
    font-weight: 800;
    color: #b26a00;
  }

  .renew-btn {
    background: #d90000;
    color: #fff;
    border-radius: 14px;
    padding: 14px;
    font-weight: 700;
    width: 100%;
    border: none;
  }
</style>

<body class="bg-light">
  <%- include('../partials/parentNavbar') %>

    <div class="main-content">
      <div class="container px-lg-4">

        <div class="row mb-4">
          <div class="col-12">
            <div
              class="card shadow-sm border-0 rounded-4 p-4 d-flex flex-row align-items-center justify-content-between"
              style="background:#fff7f7;">
              <div class="d-flex align-items-center gap-4">
                <div class="rounded-4 d-flex align-items-center justify-content-center text-white fw-bold"
                  style="width:64px;height:64px;background:#940707;font-size:22px;">
                  <%= (student?.fullName || 'S' )[0] %>
                    <%= (student?.fullName || 'T' ).split(' ')[1]?.[0] || '' %>
            </div>
            <div>
              <h4 class="fw-bold mb-1"><%= student?.fullName || ' Student Name' %>
                      </h4>
                      <div class="text-muted small">
                        Coordinator:
                        <strong>
                          <%= student?.coordinator?.fullName || '-' %>
                        </strong>
                      </div>
                </div>
              </div>
            </div>
          </div>
        </div>


        <div class="row g-4 mb-4">

          <div class="col-12 col-lg-8">

            <div class="performance-card d-flex align-items-center justify-content-between mb-4">
              <div>
                <small class="fw-semibold text-uppercase">
                  <i class="bi bi-award me-1 performancebadge"></i> Overall Performance
                </small>
                <h2 class="mt-2 mb-0">
                  <%= avgPerformance ?? 0 %>%
                </h2>
              </div>

              <div class="rounded-circle d-flex align-items-center justify-content-center" style="width:70px;height:70px;border:5px solid rgba(255,255,255,.9);
           font-size:20px;font-weight:800;">
                <%= avgPerformance>= 90 ? 'A+' :
                  avgPerformance >= 80 ? 'A' :
                  avgPerformance >= 70 ? 'B' : 'C' %>
              </div>
            </div>


            <div class="card shadow-lg border-0 rounded-4 overflow-hidden h-60">

              <div class="d-flex justify-content-between align-items-center px-4 py-3 border-bottom">
                <div class="d-flex align-items-center gap-3">
                  <div class="bg-danger text-white rounded-circle d-flex align-items-center justify-content-center"
                    style="width:44px;height:44px;">
                    <i class="bi bi-file-earmark-text fs-5"></i>
                  </div>
                  <div>
                    <h6 class="fw-bold mb-0">Latest Report Card</h6>
                    <small class="text-muted report-summary">Academic Summary</small>

                  </div>
                </div>

                <small class="fw-semibold report-date">
                  <i class="bi bi-calendar-event me-1 text-danger"></i>
                  <%= latestReport ? new Date(latestReport.createdAt).toDateString() : '-' %>
                </small>
              </div>

              <div class="row g-3 p-4">

                <div class="col-5 col-md-4">
                  <div class="h-100 text-white rounded-4 p-3"
                    style="background: radial-gradient(circle at top right, #222, #000);">
                    <small class="fw-semibold text-uppercase">Score</small>
                    <div class="fs-2 fw-bold mt-2">
                      <%= latestReport?.score || avgPerformance || 0 %>%
                    </div>
                  </div>
                </div>

                <div class="col-7 col-md-8">
                  <div class="h-100 border rounded-4 p-3">
                    <small class="fw-semibold text-uppercase text-muted">
                      Co-ordinator's Remarks
                    </small>
                    <p class="mt-2 mb-0 text-muted small">
                      <%= latestReport?.remarks || 'No remarks available.' %>
                    </p>
                  </div>
                </div>

              </div>

            </div>
          </div>

          <div class="col-12 col-lg-4">
            <div class="package-card h-100">
              <h6 class="fw-bold mb-3">
                <i class="bi bi-box-seam me-1"></i> Package Status
              </h6>

              <div class="plan-box mb-3">
                <h6 class="fw-bold mb-0">Current Plan</h6>
              </div>

              <div class="p-3 border rounded-4 d-flex align-items-center gap-3 mb-3">
                <i class="bi bi-calendar-event fs-4 text-danger"></i>
                <div>
                  <small class="text-muted">Expires On</small>
                  <div class="fw-bold">
                    <%= student?.package?.endDate ? new Date(student.package.endDate).toDateString() : 'N/A' %>
                  </div>
                </div>
              </div>

              <div class="days-left">
                <small class="fw-semibold">HOURS LEFT</small><br />
                <span>
                  <%= hoursLeft || 0 %>
                </span>
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
</body>
</html>